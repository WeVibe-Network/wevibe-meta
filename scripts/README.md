# wevibe-meta scripts

Scripts in this directory are workspace-level helpers, typically invoked
from the top-level Makefile.

- `apply-branch-protection.sh` - applies branch protection to all 10 product
  repos via the GitHub API (added in CO-288)
